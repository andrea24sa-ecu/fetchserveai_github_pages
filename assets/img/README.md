Place your new hero image file in this folder with the filename `ai-anywhere.png`.

Recommended steps:

- Add the image file: `assets/img/ai-anywhere.png` (PNG, JPEG or WebP supported).
- The page references `assets/img/ai-anywhere.png`; update the filename in `index.html` if you choose a different name.
- For best results use a 1200–1600px wide image with a transparent or light background.

If you want me to add the actual image directly, upload it here or paste the image as a base64 data URL and tell me the filename you prefer.

